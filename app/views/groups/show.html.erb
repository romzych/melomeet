<h3>Nom du group : <%= @group.name %></h3>
<p>Déscription : <%= @group.body %></p>
<p>Créateur du groupe : <%= @group.admin.email %></p>

<% if user_signed_in? %>

<%= link_to "rejoindre le group",  controller: "join_groups", action: "create", id: @group %>
<% end %>